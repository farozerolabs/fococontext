{
  "name": "fococontext",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.5.2",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=11.0.0"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "docs:build": "pnpm --filter @fococontext/docs-site build",
    "docs:dev": "pnpm --filter @fococontext/docs-site dev",
    "docs:preview": "pnpm --filter @fococontext/docs-site preview",
    "docs:validate": "pnpm --filter @fococontext/docs-site docs:validate",
    "docker:cleanup": "node scripts/docker-cleanup.mjs",
    "docker:up": "node scripts/docker-dev-up.mjs",
    "docker:up:ocr": "node scripts/docker-dev-up.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint . --ext .js,.mjs,.cjs,.ts,.tsx",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -b --pretty false",
    "validation:black-box": "node scripts/validation/white-box-real-data-validation.mjs --compose true --runtime true --openapi true --admin true --start-compose true",
    "validation:release": "node scripts/validation/white-box-real-data-validation.mjs --start-compose true",
    "validation:report-contract": "node scripts/validation/release-validation-report-contract.mjs --self-test",
    "validation:white-box": "node scripts/validation/white-box-real-data-validation.mjs --compose true --runtime true --openapi false --admin false",
    "verify": "pnpm run format:check && pnpm run lint && pnpm run typecheck && pnpm run test"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.9.2",
    "eslint": "^10.4.1",
    "prettier": "^3.8.3",
    "prettier-plugin-tailwindcss": "^0.8.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.60.1",
    "vitest": "^4.1.8"
  }
}
